{
  "name": "shopify-discount-sync",
  "version": "1.0.0",
  "description": "Multi-store Shopify discount eligibility sync via webhooks",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@shopify/shopify-api": "^9.0.0",
    "node-fetch": "^3.3.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}
